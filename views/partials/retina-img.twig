<img src="{{img.src(size) | retina(0.25)}}" data-sizes="auto" data-src="{{img.src(size)}}" width="{{width}}" height="{{height}}" alt="{{alt}}" title="{{title}}" class="{{class}}" data-srcset="
    {{img.src(size) | retina(0.5)}} {{width * 0.5}}w,
    {{img.src(size) | retina(1.0)}} {{width * 1.0}}w,
    {{img.src(size) | retina(1.5)}} {{width * 1.5}}w,
    {{img.src(size) | retina(2.0)}} {{width * 2.0}}w,
    {{img.src(size) | retina(3.0)}} {{width * 3.0}}w,
    {{img.src(size) | retina(4.0)}} {{width * 4.0}}w">